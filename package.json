{
  "name": "takeoff-tooling",
  "private": true,
  "description": "Static web app for electrical estimators — manifest bids, MC labor/price book, supplier price updates.",
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "test:unit": "node --test",
    "test": "playwright test",
    "check": "npm run lint && npm run test:unit"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@playwright/test": "^1.49.0",
    "eslint": "^9.39.4",
    "globals": "^17.6.0"
  }
}

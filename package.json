{
  "name": "takeoff-tooling",
  "private": true,
  "description": "Static web app for electrical estimators — manifest bids, MC labor/price book, supplier price updates.",
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "test:unit": "node --test",
    "build:shared": "node scripts/build-shared.js",
    "test": "playwright test",
    "check": "npm run lint && npm run test:unit && node scripts/build-shared.js --check"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@playwright/test": "^1.49.0",
    "eslint": "^9.39.4",
    "globals": "^17.6.0"
  }
}
